#include<stdio.h>
#define LIMIT 100000
int main()
{
	//Program to run for 30s
	int i,j,k;
	for(i=0;i<LIMIT;i++)
		for(j=0;j<LIMIT;j++);

	printf("Input\n");
	scanf("%d",&k);

	for(i=0;i<LIMIT;i++)
		for(j=0;j<LIMIT;j++);

	return 0;
}